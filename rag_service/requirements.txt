# Mirage RAG service — pinned dependencies.
# Tested with Python 3.11.
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
requests==2.32.3
numpy==2.1.3
# SentenceTransformers is optional (only needed for the local embedding
# path). Install with:  pip install -r requirements.txt sentence-transformers==3.3.1
sentence-transformers==3.3.1
